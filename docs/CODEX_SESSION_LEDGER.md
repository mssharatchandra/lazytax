# Codex Build Week session ledger

> Public-safe submission evidence, recorded 21 July 2026. This repository is
> public. The source tasks include real taxpayer documents and one password-like
> identifier, so this ledger deliberately records product/build intent without
> reproducing raw filenames, local paths, identifiers, secrets, tax amounts, or
> document contents.

## `/feedback` answer for Devpost

Use this value for submission field `27950`, **“/feedback Session ID where the
majority of your project was worked on”**:

```text
019f7a12-3155-7fc2-bcd9-7cc5f2078de6
```

This is the primary Build Week task. It contains the scope lock, product thesis,
security and architecture decisions, plugin-first pivot, implementation,
deterministic engine and MCP work, Tax Proof Pack, practitioner direction,
Trust Lab, unit/integration/Playwright verification, plugin installation,
GitHub publication, and CI release gate.

Run `/feedback` as a standalone command in that Codex task before submitting the
form. The task ID above is the repository record; the command is the user-owned
action that sends the task feedback through Codex.

## Sessions included

| Role | Codex session ID | Public-safe description |
|---|---|---|
| Primary build session | `019f7a12-3155-7fc2-bcd9-7cc5f2078de6` | Majority of product strategy, engineering and release work |
| Supporting private validation | `019f807a-16e1-7d92-9c37-a7d9c2a7c35f` | Local Form 16 organization, gap analysis and supervised portal handoff attempt |
| Supporting plugin validation | `019f7b77-7af7-7690-b9bc-4ab6f80ac6c6` | Private-mode multi-employer Form 16 reconciliation and filing checklist |

The two supporting sessions are not recommended for the `/feedback` field:
they validate the real-user workflow, but the primary session contains the
majority of the core implementation and has the clearest Build Week provenance.

## Primary-session input ledger

The following preserves every user input's intent in chronological order. Text
is summarized only where verbatim publication would disclose private data or a
large pasted transcript.

1. Continue the Taxly/LazyTax product-scope plan and finish actionable Linear
   ticket creation.
2. Audit the product, articulate its right-to-win thesis and assess whether it
   should be a Codex plugin or MCP server.
3. Define data-law security protocols and the technology stack; add MCP, Codex
   and Claude Code interfaces to the MVP roadmap and update Linear.
4. Position the product to win OpenAI Build Week on Devpost and select a track.
5. Consider a Codex/ChatGPT plugin as the lowest-friction, OpenAI-native product.
6. Lock the Build Week MVP around an installable plugin, one excellent tax
   skill, local MCP, deterministic supported profile, synthetic fixtures,
   reconciliation, Tax Proof Pack, tests, judge guide and three-minute demo;
   begin building immediately with parallel agents.
7. Make the product more robust, consider privacy-safe analytics and plugin
   distribution, and plan broader Indian-tax coverage without a product gap.
8. Test the local plugin against an explicitly authorized private Form 16,
   without filing, and explain how AIS/portal capabilities work without exposing
   identifiers. **Private filename and local path withheld.**
9. Supplied a password-like tax identifier for a private document. **Entire
   value withheld; it must never be committed or used as public evidence.**
10. Added three private tax/broker workbooks. **Filenames and paths withheld.**
11. Supplied private taxpayer profile context and noted that one broker source
    was already included; asked whether the plugin worked correctly. **Personal
    profile details withheld.**
12. Asked how to make the plugin genuinely usable by real taxpayers.
13. Asked whether a full-stack application or plugin would best combine real
    users, usability and hackathon victory.
14. Locked the execution order: proof integrity, PII/dedup/mixed-taxpayer
    controls, real MCP-backed viewer, hosted judge sandbox, installable plugin,
    minimal upload/consent, then OAuth/encrypted beta and ERI integration.
15. Reported that mandatory redaction created unacceptable friction and pasted
    a failed private-plugin transcript. The key product input was to support
    real data securely without repeatedly blocking the user. **Transcript,
    filenames and taxpayer data withheld.**
16. Asked to include supported US-stock tax preparation rather than leave a
    known coverage gap.
17. Approved continuing the implementation.
18. Pivoted the experience toward an experienced CA-style companion that does
    the filing legwork, securely handles real data and government-site workflows,
    and asks only for facts it cannot discover safely.
19. Extended positioning to existing CAs and tax agents as an AI-junior workflow,
    alongside the taxpayer concierge.
20. Directed implementation of the shared CA/taxpayer vision, synchronization
    of Linear tickets, progress capture in `AGENTS.md`, and a production-quality
    direction lock.
21. Asked whether the repository had been pushed to GitHub.
22. Authorized making the repository public and pushing it.
23. Explored global, country-pack-based tax expansion for the hackathon story.
24. Asked what LazyTax accomplishes that Codex independently does not—the core
    differentiation question.
25. Directed implementation and push of thorough verification, unit tests,
    Playwright tests, a sandbox environment and high-signal winning-product proof.
26. Asked whether the product could be live-demonstrated locally with private
    Form 16s.
27. Requested that all completed work be pushed to the repository.
28. Requested the `/feedback` session ID and a public repository ledger covering
    this session and the two named supporting sessions.

## Supporting-session input ledger

### `019f807a-16e1-7d92-9c37-a7d9c2a7c35f`

1. Invoked the installed LazyTax plugin to take over tax-document organization,
   identify missing evidence and guide the filing workflow.
2. Authorized two exact private Form 16 PDFs for local review. **Filenames,
   paths, identifiers, employers and amounts withheld.**

This task tested the intended concierge behavior: local document inventory,
duplicate/taxpayer checks, multi-employer reasoning, authoritative-source gap
identification and a user-controlled official-portal login handoff. It was
interrupted before a complete filing flow and is supporting evidence only.

### `019f7b77-7af7-7690-b9bc-4ab6f80ac6c6`

1. Invoked the installed LazyTax plugin with two exact private Form 16 PDFs and
   asked it to organize the documents, find missing evidence and guide the case
   to completion. **Filenames, paths, identifiers, employers and amounts
   withheld.**

This task exercised the private normalization and reconciliation path, detected
that the certificates represented different employers rather than duplicates,
created a masked working checklist and stopped at the taxpayer-controlled portal
authentication boundary. It did not submit, pay or e-verify a return.

## Publication policy

- This ledger contains session IDs because the project owner explicitly asked
  for them to be published as Build Week provenance.
- Raw task exports are intentionally not committed. They would expose private
  tax data and add no legitimate judging value.
- Judges can reproduce the public evidence using the bundled synthetic fixtures,
  `npm run check:full`, the installable plugin and the executable Trust Lab.
- The repository's public claims remain limited to implemented, tested behavior.
